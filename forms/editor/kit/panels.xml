<?xml version="1.0"?>
<panels>
	<panel name="resume"/>
	<panel name="properties"/>
	<panel name="kititems" icon="product" labeli18n="m.catalog.bo.doceditor.tab.kititems" />
	<panel name="publication"/>
	<panel name="localization" />
	<panel name="history"/>
	<panel name="redirect" />
	<panel name="create"/>
	<xul>
		<javascript>
			<constructor><![CDATA[
				if (getController().hasModule('comment'))
				{
					this.addTab('comments', '&modules.comment.bo.doceditor.tab.Comments;', 'comments', 'history');
				}
				if (getController().hasModule('twitterconnect'))
				{
					this.addTab('tweets', "&modules.twitterconnect.bo.doceditor.tab.Tweets;", 'tweet', 'history');
				}
			]]></constructor>
		</javascript>
	</xul>	
</panels>