<?xml version="1.0"?>
<panels>
	<panel name="resume"/>
	<panel name="properties"/>
	<panel name="declinations" icon="product" labeli18n="m.catalog.bo.doceditor.tab.declinations" />
	<panel name="prices" icon="price" labeli18n="m.catalog.bo.doceditor.tab.prices" />
	<panel name="publication"/>
	<panel name="localization" />
	<panel name="history"/>
	<panel name="create"/>
	<xul>
		<javascript>
			<constructor><![CDATA[
				if (getController().hasModule('comment'))
				{
					this.addTab('comments', "${transui:m.comment.bo.doceditor.tab.comments,js,ucf}", 'comments', 'history');
				}
				if (getController().hasModule('twitterconnect'))
				{
					this.addTab('tweets', "${transui:m.twitterconnect.bo.doceditor.tab.tweets,js,ucf}", 'tweet', 'history');
				}
			]]></constructor>
		</javascript>
	</xul>	
</panels>